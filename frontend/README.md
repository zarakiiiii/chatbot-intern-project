# Frontend (WhatsApp-style Chat)

Open `frontend/index.html` directly in a browser, or serve locally:

```bash
# Python
python -m http.server 8080
# then open http://127.0.0.1:8080/frontend/
```

Make sure FastAPI is running at `http://127.0.0.1:8000`.
